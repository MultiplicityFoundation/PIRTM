# ETP Pass 2 — ADR-009/007 (Docs + Tests)

- Type: execution PR pass
- Date: 2026-03-01
- Scope: second concrete implementation slice from accepted-candidate sequence

## Included ADRs

1. ADR-009 — Certificate type promotion and migration discipline
2. ADR-007 — Design-parameter commitment fail-closed behavior

## Deliverables

- Migration compatibility evidence expanded:
  - Legacy-field/key compatibility assertions added for `ace_certificate(...)` and legacy mapping path.
  - `legacy_ace_certificate(...)` parity checks added for baseline legacy fields.
- Envelope fail-closed evidence expanded:
  - Deterministic protocol-boundary failures added for injected clamp-envelope and perturbation-envelope violations.
- ADR and migration docs updated with pass-specific execution notes.

## Validation Commands

```bash
pytest -q tests/test_certify.py tests/test_ace_protocol_injection.py tests/test_ace_protocol.py
```

## Exit Criteria

- All targeted tests pass.
- No regression in existing ACE protocol behavior.
- Candidate ADR text, migration docs, and tests remain consistent.
